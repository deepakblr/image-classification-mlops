# Inference-only deps for the Docker image.
# PyTorch CPU wheels are installed separately in docker/Dockerfile (avoids CUDA deps on CI).
Pillow==10.4.0
numpy==1.26.4
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.17
prometheus-client==0.21.0
pydantic==2.10.2
